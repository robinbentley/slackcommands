general:
  branches:
    only:
      - rework

machine:
  node:
    version: 6.1.0
  environment:
    URBAN_TOKEN: FAKETOKEN

deployment:
  production:
    branch: rework
    heroku:
      appname: rbslackcommands
