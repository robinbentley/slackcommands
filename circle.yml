general:
  branches:
    only:
      - rework

machine:
  node:
    version: 6.1.0
  environment:
    URBAN_TOKEN: FAKETOKEN

test:
  post:
    - npm run build

deployment:
  producttion:
    branch: rework
    heroku:
      appname: rbslackcommands
