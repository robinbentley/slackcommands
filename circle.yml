general:
  branches:
    only:
      - master

machine:
  node:
    version: 6.9
  environment:
    URBAN_TOKEN: URBAN

deployment:
  production:
    branch: master
    heroku:
      appname: rbslackcommands
